"""
Агент «ранковий дайджест пошти в Telegram».

.env читається тут, на рівні пакета, а не в config.py: інструменти беруть
ключі просто з os.environ, і покладатися на те, що хтось раніше імпортував
config, — це вада, яка вилазить лише при прямому імпорті одного модуля.
"""

from dotenv import load_dotenv

load_dotenv()
